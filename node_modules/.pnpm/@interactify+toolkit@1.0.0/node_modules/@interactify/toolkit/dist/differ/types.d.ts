export interface IResult<T> {
    prevList: T[];
    list: T[];
    added: number[];
    removed: number[];
    changed: Array<[number, number]>;
    maintained: Array<[number, number]>;
    changedBeforeAdded: Array<[number, number]>;
    fixed: boolean[];
    ordered: Array<[number, number]>;
    pureChanged: Array<[number, number]>;
}
